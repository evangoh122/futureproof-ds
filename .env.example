# Database secrets only. Everything else about the connection (host, port,
# database name, schema, table) lives in config.yaml under `database:`.
# Format is KEY=value — python-dotenv cannot parse "Key: value".
Username=
Password=
